TODO
create a new script that can launch on prof's laptop. Only this script should be called in prof's laptop

script should generate required config files

Arguments:
script should take in image-id (clean image id specified by prof)
requirements - install fire (need to add in documentation)

script should do the following in order:

1. generate key pair. Then Change permissions of the keypair (maybe just take in keypair name as string. Specify to not include ".pem" in the name)
2. generate security group with correct permissions
3. launch instances with key pair and security group and specified image id
4. on return from launching instances with image id, should generate appropriately named IP.txt files
5. launch respective scripts to initialize and install relevant software the 4 servers.
6. after and installation of software, script should scp the generated IP.txt files to the correct servers and override them